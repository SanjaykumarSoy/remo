#include <stdio.h>

int main() {
    int first ,second,*p,*q,sum;
    first =1, second =2;
    p=&first;
    q=&second;
    sum =*p + *q;
    printf("the sum of %d and %d= %d", first ,second ,sum);
    return 0;
}
